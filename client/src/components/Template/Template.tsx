import { FC } from "react";

interface Props {
  string: string;
}
const Template: FC<Props> = ({ string }) => {
  return (
    <div>
      <div>Template</div>
    </div>
  );
};

export default Template;

/*** Notes ***
 * Notes go here.
 */
